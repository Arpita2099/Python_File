# Set is an unordered collection of items.
# Every set element is unique (no duplicate value).
# Set is immutable value (can not be changed).

s={10,20,30,40,50}
print(s,type(s)) # It will be output: {50, 20, 40, 10, 30} <class 'set'>
